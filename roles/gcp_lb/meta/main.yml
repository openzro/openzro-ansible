---
# Collection deps live in /requirements.yml (the `collections:` key
# in meta/main.yml only accepts strings, not the versioned dict form).

galaxy_info:
  author: openZro
  description: >-
    Provision an external HTTPS LB (control plane) and an external
    Network LB (relay tier) for openZro on GCP. Runs from localhost
    via the GCP API.
  license: BSD-3-Clause
  min_ansible_version: "2.15"
  platforms:
    - name: GenericLinux
      versions: [all]
